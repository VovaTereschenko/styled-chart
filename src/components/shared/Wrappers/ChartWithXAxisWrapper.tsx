import styled from 'styled-components';

const ChartWithXAxisWrapper = styled.section`
  display: block;
  flex-wrap: wrap;
  align-content: flex-start;
  width: 100%;
`;

export default ChartWithXAxisWrapper;
