import styled from 'styled-components'
import constants from '../../../constants'

const BarGroup = styled.div`
  display: flex;
  flex-direction: column;
  align-self: flex-end;
  background: ${constants.defaultBargroupColor};
`

export default BarGroup
