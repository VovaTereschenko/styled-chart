import styled from 'styled-components';

const EmptyAreaWrapper = styled.section`
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
`;

export default EmptyAreaWrapper;
